# Scenes

| Scene | Purpose |
|---|---|
| `titlemenu` | Title / main menu |
| `charselect` | Character selection |
| `main` (MouseButton) | Primary gameplay |

`ZoneLoader` handles in-game zone transitions. `GameManager` (DontDestroyOnLoad singleton) holds `playerTransform`, `cursorTransform`, and `TrolManager` references.
